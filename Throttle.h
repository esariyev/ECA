#ifndef THROTTLE_H
#define THROTTLE_H

// Function to initialize the Throttle BSW module
void Throttle_Init(void);

// Function to read the position of the throttle pedal
float Throttle_GetPosition(void);

#endif // THROTTLE_H
